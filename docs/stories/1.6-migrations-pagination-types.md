# Story 1.6: DB migrations + paginacao + tipos

| Campo | Valor |
|-------|-------|
| **ID** | 1.6 |
| **Epic** | EPIC-1 (Technical Debt) |
| **Status** | Draft |
| **Points** | 8 |
| **Prioridade** | P2 - Media |
| **Executor** | @dev |
| **Quality Gate** | @architect |
| **Debitos** | DB-2, API-1, TYPE-1 |

---

## Story

**Como** developer do ProspectAI,
**Quero** que o schema do banco seja versionado, os resultados paginados e os tipos TypeScript precisos,
**Para que** o projeto escale de forma controlada e segura.

---

## Acceptance Criteria

- [ ] AC1: Schema inicial migrado para `lib/supabase/migrations/001_initial_schema.sql`
- [ ] AC2: UPDATE policies incluidas na migration
- [ ] AC3: API `/api/prospect` aceita parametros `page` e `limit` (default: page=1, limit=20)
- [ ] AC4: Response inclui `{ leads, total, page, limit, totalPages }`
- [ ] AC5: Frontend exibe paginacao (anterior/proximo) nos resultados
- [ ] AC6: Tipos `any[]` substituidos por tipos concretos em `types/index.ts`
- [ ] AC7: `regularOpeningHours`, `photos`, `reviews` tipados corretamente

---

## Scope

### In
- Migracao do schema.sql para migrations numeradas
- Paginacao server-side na API de busca
- Componente de paginacao no frontend
- Tipagem forte para campos `any`

### Out
- Cursor-based pagination (offset-based e suficiente para MVP)
- Historico de buscas com paginacao
- Infinite scroll

---

## Tasks

- [ ] **Task 1:** Criar migration inicial
  - Mover conteudo de `schema.sql` para `migrations/001_initial_schema.sql`
  - Adicionar UPDATE policies
  - Adicionar header com metadata (data, descricao)
  - (AC1, AC2)

- [ ] **Task 2:** Implementar paginacao na API
  - Em `/api/prospect/route.ts`:
  - Aceitar query params `page` (default 1) e `limit` (default 20, max 50)
  - Paginar array de leads antes de retornar
  - Incluir metadata de paginacao no response
  - (AC3, AC4)

- [ ] **Task 3:** Componente de paginacao no frontend
  - Em `ResultsList.tsx`:
  - Adicionar botoes "Anterior" / "Proxima"
  - Exibir "Pagina X de Y"
  - Atualizar hook para suportar paginacao
  - (AC5)

- [ ] **Task 4:** Tipar campos `any`
  - Em `types/index.ts`:
  - Criar tipo `OpeningHours` para `regularOpeningHours`
  - Criar tipo `PlacePhoto` para `photos`
  - Criar tipo `PlaceReview` para `reviews`
  - Substituir `any` pelos tipos concretos
  - (AC6, AC7)

---

## Dev Notes

**Tipos Google Places (referencia):**
```typescript
type OpeningHours = {
  weekdayDescriptions?: string[];
  periods?: { open: { day: number; hour: number; minute: number }; close?: { day: number; hour: number; minute: number } }[];
};

type PlacePhoto = {
  name?: string;
  widthPx?: number;
  heightPx?: number;
};

type PlaceReview = {
  rating?: number;
  text?: { text: string; languageCode?: string };
  relativePublishTimeDescription?: string;
};
```

---

## Change Log

| Data | Versao | Descricao | Autor |
|------|--------|-----------|-------|
| 2026-03-06 | 1.0 | Story criada | @architect |
