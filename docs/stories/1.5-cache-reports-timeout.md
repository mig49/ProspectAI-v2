# Story 1.5: Cache de reports + timeout Gemini

| Campo | Valor |
|-------|-------|
| **ID** | 1.5 |
| **Epic** | EPIC-1 (Technical Debt) |
| **Status** | Ready for Review |
| **Points** | 3 |
| **Prioridade** | P1 - Alta |
| **Executor** | @dev |
| **Quality Gate** | @architect |
| **Debitos** | PERF-1, PERF-2 |

---

## Story

**Como** usuario do ProspectAI,
**Quero** que reports ja gerados sejam servidos instantaneamente e que buscas nao travem indefinidamente,
**Para que** a experiencia seja rapida e confiavel.

---

## Acceptance Criteria

- [ ] AC1: Reports gerados sao salvos na coluna `detailed_report` da tabela `leads`
- [ ] AC2: Se `detailed_report` ja existe para um lead, e retornado sem chamar Gemini
- [ ] AC3: Botao "Regenerar Relatorio" permite forcar nova geracao
- [ ] AC4: Chamadas Gemini tem timeout de 60 segundos
- [ ] AC5: Timeout resulta em mensagem de erro amigavel (nao trava a UI)
- [ ] AC6: Cache reduz chamadas Gemini em revisitas ao mesmo lead

---

## Scope

### In
- Persistencia de reports na coluna `detailed_report` (ja existe no schema)
- Check de cache antes de chamar Gemini
- AbortController com timeout de 60s
- Botao de regenerar no frontend

### Out
- TTL/expiracao de cache (pode ser v2)
- Cache em Redis/Edge
- Streaming de responses

---

## Dependencies

- **Pre-requisitos:** Story 1.1 (ownership check com external_id — o cache usa o mesmo flow)
- **Bloqueada por:** Nenhuma (1.1 ja esta Done)
- **Bloqueia:** Nenhuma

---

## Risk Assessment

| Risco | Mitigacao |
|-------|-----------|
| SDK @google/genai pode nao suportar AbortSignal | Usar Promise.race com timeout promise como fallback |
| Cache pode servir reports desatualizados | Botao "Regenerar" resolve (AC3); TTL pode ser adicionado no futuro |
| Ownership check ja usa external_id (Story 1.1) — Tasks devem alinhar | Task 1 deve usar external_id (nao lead DB id) para cache lookup |

---

## Tasks

- [x] **Task 1:** Implementar cache de reports no backend
  - Em `/api/prospect/report/route.ts`:
  - Receber `lead_id` no request body
  - Query Supabase: `leads.select('id, detailed_report').eq('external_id', leadId).eq('user_id', user.id).single()`
  - Se `detailed_report` nao null E nao `force_regenerate`, retornar cached
  - Se null ou force, gerar via Gemini e salvar com `leads.update({ detailed_report }).eq('id', dbLead.id)`
  - (AC1, AC2)

- [x] **Task 2:** Adicionar timeout nas chamadas Gemini
  - Em ambos endpoints (`/api/prospect` e `/api/prospect/report`):
  - Usar `AbortController` com `setTimeout(60000)`
  - Wrap chamada Gemini com abort signal
  - Catch `AbortError` e retornar 504 com mensagem
  - (AC4, AC5)

- [x] **Task 3:** Botao regenerar no frontend
  - Em `LeadDetail.tsx`: adicionar botao "Regenerar" ao lado de "Copiar"
  - Passar `force_regenerate: true` no body do request
  - Em `hooks/use-lead-report.ts`: aceitar param `forceRegenerate`
  - (AC3)

- [x] **Task 4:** Atualizar hook para suportar cache
  - `use-lead-report.ts`: passar `lead_id` no request body
  - Manter backward-compatible com flow existente
  - (AC6)

---

## Dev Notes

**Coluna `detailed_report`:** Ja existe no schema (`detailed_report TEXT`), esta pronta para uso.

**Timeout pattern:**
```typescript
const controller = new AbortController();
const timeout = setTimeout(() => controller.abort(), 60000);
try {
  const result = await genAI.models.generateContent({ ...config, signal: controller.signal });
} finally {
  clearTimeout(timeout);
}
```

**Nota:** O SDK @google/genai pode nao suportar AbortSignal diretamente. Alternativa: `Promise.race` com timeout promise.

---

## Testing

- Gerar report para lead sem cache → Gemini chamado, `detailed_report` salvo no DB
- Revisitar mesmo lead → report retornado do cache, sem chamada Gemini
- Clicar "Regenerar" → Gemini chamado novamente, cache atualizado
- Simular timeout (mock lento) → mensagem de erro amigavel, UI nao trava

---

## Change Log

| Data | Versao | Descricao | Autor |
|------|--------|-----------|-------|
| 2026-03-06 | 1.0 | Story criada | @architect |
| 2026-04-07 | 1.1 | Validacao PO: add Dependencies, Risk Assessment, Testing, fix external_id alignment | @po |
| 2026-04-07 | 1.2 | Tasks 1-4 completas: cache DB, timeout 60s, botao regenerar, testes atualizados | @dev |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### File List
| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `app/api/prospect/report/route.ts` | Modificado | Cache lookup, save detailed_report, timeout 60s, forceRegenerate |
| `app/api/prospect/route.ts` | Modificado | Timeout 60s com withTimeout() |
| `lib/validation.ts` | Modificado | forceRegenerate field no schema |
| `hooks/use-lead-report.ts` | Modificado | forceRegenerate param, regenerate() function |
| `components/LeadDetail.tsx` | Modificado | Botao "Regenerar" com RefreshCw icon |
| `app/api/prospect/report/__tests__/route.test.ts` | Modificado | Testes de cache, regenerate, update mock |

---

## QA Results

| Campo | Valor |
|-------|-------|
| **Reviewer** | @qa (Quinn) |
| **Data** | 2026-04-07 |
| **Veredicto** | PASS |
| **Iteracoes** | 1 |

**Resumo:** Cache via detailed_report funcional, timeout 60s em ambos endpoints, botao Regenerar no frontend. Todos os 6 ACs verificados.
