# Story 2.1: Stripe integration + checkout

| Campo | Valor |
|-------|-------|
| **ID** | 2.1 |
| **Epic** | EPIC-2 (Monetizacao v2) |
| **Status** | Ready for Review |
| **Points** | 5 |
| **Prioridade** | P0 - Critica |
| **Executor** | @dev |
| **Quality Gate** | @qa |

---

## Story

**Como** owner do ProspectAI,
**Quero** integrar o Stripe para processar pagamentos de assinaturas,
**Para que** usuarios possam assinar os planos Pro e Scale de forma segura e automatizada.

---

## Acceptance Criteria

- [x] AC1: Stripe SDK instalado e configurado com chaves em env vars (STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET)
- [x] AC2: Dois Products criados no Stripe (Pro 29 EUR/mes, Scale 79 EUR/mes) com Price IDs referenciados no codigo
- [x] AC3: Rota POST `/api/stripe/checkout` cria Checkout Session com o plano selecionado e redireciona para Stripe
- [x] AC4: Rota POST `/api/stripe/webhook` recebe eventos do Stripe (checkout.session.completed, customer.subscription.updated, customer.subscription.deleted)
- [x] AC5: Webhook cria/atualiza registro na tabela `subscriptions` do Supabase
- [x] AC6: Rota GET `/api/stripe/portal` cria sessao do Customer Portal para gerenciamento da assinatura
- [x] AC7: Todos os usuarios novos iniciam no plano `free` automaticamente
- [x] AC8: Testes unitarios cobrem criacao de checkout, processamento de webhook e fallback de erros

---

## Scope

### In
- Instalacao do `stripe` npm package
- API routes: `/api/stripe/checkout`, `/api/stripe/webhook`, `/api/stripe/portal`
- Tabela `subscriptions` no Supabase
- Env vars para Stripe keys
- Utility `lib/stripe.ts` com client e helpers

### Out
- UI da pricing page (Story 2.3)
- Paywall middleware (Story 2.2)
- Emails transacionais de confirmacao

---

## Technical Notes

### Tabela Supabase

```sql
create table subscriptions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users not null unique,
  stripe_customer_id text,
  stripe_subscription_id text,
  plan text check (plan in ('free','pro','scale')) default 'free',
  status text check (status in ('active','canceled','past_due','trialing')) default 'active',
  current_period_end timestamptz,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

alter table subscriptions enable row level security;
create policy "Users read own subscription" on subscriptions for select using (auth.uid() = user_id);
```

### Stripe Webhook Events

| Event | Action |
|-------|--------|
| `checkout.session.completed` | Create subscription record, set plan |
| `customer.subscription.updated` | Update plan/status/period_end |
| `customer.subscription.deleted` | Set plan to 'free', status to 'canceled' |
| `invoice.payment_failed` | Set status to 'past_due' |

### API Route Structure

```
app/api/stripe/
  checkout/route.ts    — POST: create checkout session
  webhook/route.ts     — POST: handle Stripe events
  portal/route.ts      — GET: create customer portal session
```

---

## Dependencies

- Conta Stripe ativa com Products/Prices criados
- Supabase migration aplicada
- Env vars configuradas no Vercel

---

## File List

- [x] `lib/stripe.ts` — Stripe client, PLANS config, getPlanByPriceId helper
- [x] `app/api/stripe/checkout/route.ts` — POST: create checkout session
- [x] `app/api/stripe/webhook/route.ts` — POST: handle 4 Stripe events
- [x] `app/api/stripe/portal/route.ts` — GET: customer portal session
- [x] `lib/supabase/migrations/002_subscriptions.sql` — subscriptions table + RLS
- [x] `app/api/stripe/__tests__/checkout.test.ts` — 4 tests (auth, invalid plan, create, reuse customer)
- [x] `app/api/stripe/__tests__/webhook.test.ts` — 5 tests (missing sig, invalid sig, checkout completed, deleted, payment failed)
- [x] `middleware.ts` — Updated: exclude webhook from auth middleware
